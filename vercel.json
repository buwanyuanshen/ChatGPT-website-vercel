{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python",
      "config": { "API_KEY": "your_api_key" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/app.py" }
  ]
}
